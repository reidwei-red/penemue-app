# Penemue · 视觉规范

> 定稿：2026-08-08 · 来源：项目方提供的五张概念图
> 这份规范是**原创**，不参考、不复制任何第三方项目的样式代码。

---

## 一句话定调

**深蓝近黑的夜，金色的光从黑暗里长出来。**

以诺书守望者的气质：庄严、深邃、孤高。不是科技蓝，不是纯黑极简，是**有空气感和纵深的暗**，光只在该亮的地方亮。

## 🚨 最重要的取舍：取气质，不取字面

概念图是**壁纸**，工作台是**每天要用几十次的生产力工具**。直接照搬会做出一个好看但没法用的东西：

| 图里的 | 直接照搬的后果 | 规范怎么定 |
|---|---|---|
| 大面积暗部、低对比 | 文字看不清，眼睛累 | 正文对比度必须达标（下面有硬指标） |
| 满屏星尘、光晕 | 干扰阅读，手机上更糟 | 氛围元素**只用在空状态和登录/设置页**，列表区一律干净 |
| 发光溢出（bloom） | 文字发虚，点不准 | 发光只用在**焦点态和警示态**，且是克制的外阴影 |
| 金色大面积铺 | 廉价、刺眼 | 金色是**强调色**，全屏占比不超过 5% |

**判断标准**：一屏待办列表，扫十遍不累眼 = 对了。截图发出去像壁纸但用起来眼晕 = 错了。

---

## 色板（CSS 变量，直接用这套）

```css
:root {
  /* 底 —— 深蓝近黑，蓝调略降往中性靠（阶段 11 调整），5 级 */
  --bg:        #070A10;   /* 最底层背景 */
  --bg-elev:   #0E1219;   /* 面板、卡片 */
  --bg-elev-2: #151A23;   /* 悬浮、下拉 */
  --bg-elev-3: #1C222D;   /* 弹层、嵌套卡片 */
  --bg-input:  #05070B;   /* 输入框，比底还深 */

  /* 强调 —— 阶段 11 由土黄 #D9A441 提亮为明亮琥珀 */
  --gold:        #F0B450;
  --gold-bright: #FFCF80;
  --gold-dim:    #8A6B33;
  --gold-line:   rgba(240, 180, 80, .16);   /* 发丝金线 */
  --gold-glow:   rgba(240, 180, 80, .26);   /* 焦点光晕 */

  /* 冷 —— 次要强调 */
  --crystal:     #7FA8C9;
  --crystal-dim: #4A6A85;

  /* 文字 —— 对比度是硬指标，不许为氛围调暗 */
  --text:      #F2F5F9;   /* 正文，对 --bg-elev ≥ 7:1（实测 17.16:1） */
  --text-soft: #A9B4C4;
  --text-mute: #6B7686;

  /* 语义 —— 阶段 11 标准化：红=危险、绿=完成是通用语义 */
  --danger: #F0574D;
  --warn:   #F0B450;
  --ok:     #4CC38A;

  --line:        rgba(255,255,255,.07);
  --line-strong: rgba(255,255,255,.12);
}
```

**对比度是硬要求**，不是建议。正文 `--text` 对面板 `--bg-elev` 必须 ≥ 7:1。任何为了"更有氛围"而调暗文字的改动，一律否掉。

---

## 质感规则（图里的东西怎么落到 UI 上）

**发丝金线** —— 图里最有辨识度的元素，也是最实用的：
- 卡片、面板的边框用 `1px solid var(--gold-line)`，不用灰色边框
- 分区标题下面画一条 `--gold-line` 的细线
- **不要**画装饰性的曲线、轨道 —— 那属于壁纸，不属于工作台

**光晕** —— 只在两处：
- 输入框 / 按钮的焦点态：`box-shadow: 0 0 0 3px var(--gold-glow)`
- 已过期的 DDL：一层很淡的 `--danger` 外发光，让它自己跳出来

**镜面反射、星尘、水晶** —— 只出现在这些地方：
- 页面首屏顶部一层极淡的径向渐变（模拟图里的天光），透明度 ≤ 0.06
- 空状态插画（比如"收集箱是空的"）
- **列表、表单、看板区域一律干净**，不放任何装饰

**渐变**：只用极淡的径向渐变做氛围，绝不用渐变做按钮或文字。

---

## 排版

- 字体：系统栈 `-apple-system, BlinkMacSystemFont, "PingFang SC", "Segoe UI", sans-serif`
- 正文 15px / 行高 1.65；小字 13px；标题 18-22px，字重 600
- **中文与英文数字之间留半角空格**（本项目的排版约定）
- 数字（日期、金额、计数）用 `font-variant-numeric: tabular-nums`，让列表里的数字对齐

## 间距与圆角

- 间距阶梯：4 / 8 / 12 / 16 / 24 / 32 px
- 圆角：小元素 6px，卡片 10px，弹层 14px。**不要全圆角胶囊**，那跟庄严的调性不符
- 卡片之间留 12px，呼吸感靠留白，不靠装饰

## 布局与响应式

```
桌面（≥ 900px）    左侧栏（220px，固定）+ 右主区
平板（600-899px）  左侧栏收成图标条（64px）
手机（< 600px）    侧栏隐藏，改底部 tab 栏，主区全宽
```

- 底部 tab 要留 iOS 安全区：`padding-bottom: env(safe-area-inset-bottom)`
- 手机上所有可点元素最小 44×44px（拇指够得着）
- 主区最大宽度 960px，居中 —— 宽屏上不要把一行文字拉到 2000px

## 动效

- 只做 150-200ms 的 `ease-out` 过渡，用在颜色、透明度、位移
- **不做**图里那种流星、粒子、旋转轨道的动画。每天用几十次的工具，动效多一分都是负担
- 尊重 `prefers-reduced-motion: reduce`，命中时关掉所有过渡

---

---

# 视觉增强（2026-08-08 补 · 第一版太素了）

> ⚠️ **本节推翻上面若干条克制条款**，冲突时以本节及更后面的章节为准。

> 第一版严格执行了上面的克制原则，结果做出一套**纯功能样式**：卡片是纯色块、按钮是实心色块、背景是纯色，全篇没有一处质感，跟概念图的气质完全不搭。
>
> **上面的克制原则继续有效，但要重新划界**：克制的对象是**阅读区**（列表正文、表单、数据），不是整个界面。**结构层**（背景、卡片边缘、按钮、导航、标题、空状态）该有质感就得有。

## 判断标准换成这个

不再问「有没有装饰」，改问：**这处装饰会不会落在人要读的字上？**

- 落在字上 → 不做
- 只在字的**周围和底下** → 做，而且要做足

## 逐项要求

### 1. 背景：要有纵深，不能是一块纯色

```css
body {
  background:
    /* 顶部天光，呼应概念图的穹顶 */
    radial-gradient(ellipse 120% 60% at 50% -10%, rgba(217,164,65,.07), transparent 60%),
    /* 冷侧光 */
    radial-gradient(ellipse 90% 50% at 85% 10%, rgba(127,168,201,.05), transparent 55%),
    /* 星尘：极淡的点阵，用两层不同疏密叠加，避免规律感 */
    radial-gradient(1px 1px at 20% 30%, rgba(232,237,245,.14), transparent),
    radial-gradient(1px 1px at 70% 65%, rgba(232,237,245,.10), transparent),
    radial-gradient(1px 1px at 45% 85%, rgba(217,164,65,.12), transparent),
    var(--bg);
  background-attachment: fixed;
}
```

星尘用 `background-attachment: fixed`，滚动时不跟着动——**这是「背景」而不是「内容」的关键**，跟着滚会晕。

### 2. 卡片：三层质感

每个 `.panel` / `.project-card` / `.sidework-card` / `.topic-group` / `.deadline-panel`：

- **微渐变底**：`linear-gradient(160deg, var(--bg-elev-2), var(--bg-elev))` —— 从左上到右下略微变深，模拟受光
- **顶边高光**：`::before` 画一条 1px 的横线，`linear-gradient(90deg, transparent, var(--gold-line), transparent)`，中间亮两端淡。这是从概念图的「发丝金线」直接来的
- **外阴影**：`box-shadow: 0 2px 20px rgba(0,0,0,.35)` —— 让卡片浮起来，而不是贴在背景上

### 3. 按钮：主按钮要有分量

```css
.primary {
  background: linear-gradient(180deg, var(--gold-bright), var(--gold));
  box-shadow: 0 2px 12px rgba(217,164,65,.22), inset 0 1px 0 rgba(255,255,255,.25);
}
.primary:hover  { box-shadow: 0 4px 18px rgba(217,164,65,.34), inset 0 1px 0 rgba(255,255,255,.35); }
.primary:active { transform: translateY(1px); box-shadow: 0 1px 8px rgba(217,164,65,.2); }
```

次按钮：底色改成 `linear-gradient(180deg, var(--bg-elev-2), var(--bg-elev))` + 金线边框。

### 4. 导航选中态：加一条金色指示条

选中项左侧（手机端顶部）用 `::before` 画一条 3px 的金色竖条，配一层从左往右淡出的金色背景 `linear-gradient(90deg, rgba(217,164,65,.12), transparent)`。

现在只有变色，**扫一眼看不出在哪一页**。

### 5. 标题区：加一个视觉锚点

`.view-header h1` 前面加一个小菱形（`::before`，8px 旋转 45° 的金色方块，带一点发光）。分隔线改成渐变：`linear-gradient(90deg, var(--gold), var(--gold-line) 30%, transparent)` —— 从左边亮到右边淡出。

### 6. 空状态：这里可以放开做

`.placeholder` 和「还没有 XX」这类空列表，放一个**水晶尖峰的内联 SVG 剪影**（三角形群 + 顶端一点光晕，用 `--crystal` 和 `--gold`，整体透明度 .35），下面配一句提示。

空状态是全篇唯一可以"好看"而不影响任何功能的地方，**别浪费**。

### 7. 状态色要发光

- `.deadline-row.overdue`：加 `text-shadow: 0 0 14px rgba(224,114,47,.45)`，让过期项自己跳出来
- `.calendar-day.today`：加 `box-shadow: 0 0 0 1px var(--gold), 0 0 16px rgba(217,164,65,.28)`
- 日历圆点：加对应颜色的微光 `box-shadow: 0 0 6px currentColor`

### 8. 品牌区

`PENEMUE` 用金色渐变文字（`background-clip: text`），下面那行 `守望者工作台` 保持低调。侧栏底部可以加一条极淡的渐变分隔。

## 仍然不许做（这几条不放开）

| 不许 | 为什么 |
|---|---|
| 列表正文、表单文字上加任何阴影/发光/渐变 | 那是要读的字，糊了就没法用 |
| 正文对比度低于 7:1 | 硬指标，不因为好看让步 |
| 粒子动画、流星、旋转轨道 | 每天点几十次的工具，动效多一分都是负担 |
| 装饰性 SVG 出现在列表/表单区 | 只能在空状态和标题锚点 |
| 单次过渡超过 200ms | 慢动画在高频操作里就是卡顿 |

## 新的一句话自检

**眯起眼睛看这一屏**：应该能看出明暗层次和金色的走向；但**睁开眼读字**时，每个字都清清楚楚。两个都满足才算对。
